package com.example.valorantpickercompose.domain.model
// класс для хранения констант - всех возможных ролей агентов для типобезопасности, чтобы писать не строки у агентов
enum class Role {
    CONTROLLER,
    DUELIST,
    INITIATOR,
    SENTINEL
}
